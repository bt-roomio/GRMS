from .settings import *

DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': '<TEST>',
        'USER': '<TEST>',
        'PASSWORD': '<TEST_PWD>',
        'HOST': 'localhost',
        'PORT': 5432,
    }
}

STATIC_ROOT = '/static'
MEDIA_ROOT = '/uploads